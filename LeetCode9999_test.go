package main

import (
	"fmt"
	"testing"
)

/**
序号：977
标题：有序数组的平方
日期：2022/06/03
类型：
*/
func Test9999(t *testing.T) {
	var nums = []int{-4, -1, 0, 3, 10}
	fmt.Println(sortedSquares(nums))
}

/**
给你一个按 非递减顺序 排序的整数数组 nums，返回 每个数字的平方 组成的新数组，要求也按 非递减顺序 排序。
*/
func dfs99(nums []int) []int {
	//判断是否存在
	//if node in

	//添加到已被访问到

	//处理当前节点

	//处理左子树

	//处理右子树

	return nil

}
